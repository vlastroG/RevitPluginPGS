<Window x:Class="MS.GUI.Windows.AR.LintelsManager.LintelsManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MS.GUI.Windows.AR.LintelsManager"
             xmlns:vm="clr-namespace:MS.GUI.ViewModels.AR.LintelsManager"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Window.DataContext>
        <vm:LintelsManagerViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="7*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <DataGrid ItemsSource="{Binding Openings}"
                  CanUserAddRows="True"
                  SelectedItem="{Binding SelectedOpening}">
        </DataGrid>
        <DockPanel Grid.Row="1">
            <DockPanel DockPanel.Dock="Bottom">
                <Button Content="Добавить/Редактировать перемычку"
                        Command="{Binding SetOrEditLintelCommand}"
                        CommandParameter="{Binding SelectedOpening}"/>
            </DockPanel>
        </DockPanel>
    </Grid>
</Window>
